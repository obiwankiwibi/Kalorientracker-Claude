pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/index.html
    - cp einkaufsliste.html public/einkaufsliste.html
    - cp kalorien.html public/kalorien.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
